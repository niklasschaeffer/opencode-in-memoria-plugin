{
  "$schema": "https://opencode.ai/config.json",
  "description": "OpenCode configuration with In-Memoria MCP integration",
  "mcp": {
    "in-memoria": {
      "type": "local",
      "command": ["npx", "in-memoria", "server"],
      "description": "In-Memoria persistent intelligence server for codebase analysis and pattern learning"
    }
  }
}
